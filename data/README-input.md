# Native timetable input

`school-timetable-input.xlsx` is the approved 2026 autumn school timetable workbook supplied by the user.

GitHub Actions solves this workbook directly and publishes the updated workbook as an artifact. The solver preserves the workbook layout and changes only lesson cells required to satisfy hard constraints.
